# Frontend env (Vite)
# ------------------
# Committed: .env.development (dev) and .env.production (prod).
# Optional: copy to .env for local overrides.
# Development: VITE_API_URL=http://localhost:4000 (default in .env.development)
# Production:  VITE_API_URL= empty for same-origin (in .env.production)
VITE_API_URL="http://localhost:4000"
